#include <stdio.h>
#include<ctype.h>
int main(){
    char ch;
    scanf("%c",&a);
    if(ch>='A'&&ch<='Z'){
        printf("Uppercase");
    }
    else{
        printf("Lowercase");
    }
    return 0;
}